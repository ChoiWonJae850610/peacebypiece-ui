# Codex Prompt — 0.24.34.5 Final Review and Commit

Status: `READY_FOR_CODEX`
Baseline version: `0.24.34.6`
Target implementation stream: `0.24.34.5 continuation`
Do not start: `0.24.35 — Company-wide Export Execution`

## Mandatory canonical read order

Before doing any implementation, read these documents in order:

1. `AGENTS.md`
2. `docs/codex-current-state.md`
3. `docs/project/26-final-policy-decisions-and-master-todo.md`
4. `docs/project/31-pre-codex-integrated-master-plan.md`
5. `docs/project/32-product-completion-and-ui-evidence-standard.md`
6. `docs/project/33-workorder-pdf-size-dashboard-finalization.md`
7. `docs/audits/0.24.34.6-system-admin-screen-gap-audit.md`
8. `docs/audits/0.24.34.6-customer-workspace-screen-gap-audit.md`
9. `docs/productization-roadmap.md`
10. `lib/internal/roadmap/roadmap-0.24.34.6.ts`
11. this prompt document
12. any source files and tests named below

Rules:

- Preserve the current dirty work if this is run as a continuation. Do not reset, checkout, restore, stash, or clean unless the owner explicitly requests it.
- Do not start `0.24.35`.
- Do not use Full Reset to hide legacy data or runtime defects.
- Do not access production DB/R2/Workers.
- Do not call work complete from build/typecheck/contract alone.
- User-visible UI work needs localhost browser evidence, screenshots, console/network counts, and a requirements matrix.
- For PDF and screen layout work, stop for owner visual review before final commit/push if the resulting visual output has not already been approved.

## Execution sequencing and version rule

- `0.24.34.6` is a document/audit/prompt-preparation version. It is already the planning baseline for these prompts.
- These prompts describe the remaining `0.24.34.5` productization work, but implementation must use the next owner-approved result version at the time Codex resumes. Do not silently reuse or change versions.
- Run the prompts in order: A -> B -> C -> D -> E -> Final.
- Do not execute prompts B-E until the previous prompt has reached its checkpoint and produced its source ZIP, matching repo-state, and evidence package when applicable.
- Do not merge A-E into one large implementation run unless the owner explicitly approves it after reviewing the previous checkpoint.
- `0.24.35 — Company-wide Export Execution` remains blocked until Final review and closeout are complete.

## Evidence package rule

- For any prompt that changes PDF output or visible UI, create a QA evidence ZIP outside the source ZIP.
- Do not include screenshots, PDF PNGs, traces, credentials, storageState, or QA evidence ZIPs in Git or in the `4. Newest` source ZIP.
- Record the QA evidence ZIP path and the included screenshot/PDF manifest in repo-state.
- If visual evidence is missing, keep the result as `PRODUCT_QA_INCOMPLETE` or `PRODUCT_QA_AWAITING_USER_REVIEW`; do not report it as product verified.

## Objective

After continuations A-E are implemented and owner visual review is complete, perform final verification and Git closeout.

## Strict precondition sequence

Final review is not a development prompt. It is only the closeout prompt after A, B, C, D, and E are all either completed or explicitly deferred by the owner.

Do not run final stage/commit/push if any of the following are missing:

- A checkpoint repo-state for each executed continuation,
- QA evidence ZIPs for PDF and visible UI work,
- owner visual review of PDF pages and dashboard screenshots,
- explicit owner approval to commit the resulting implementation,
- live/fixture distinction in repo-state,
- no unresolved production-risk item,
- no unresolved destructive migration requirement.

## Preconditions

Do not run this final prompt unless:

- PDF layout has been visually reviewed by the owner,
- screen captures for customer/system dashboards have been reviewed,
- same-Google returning-applicant flow is verified or explicitly deferred with owner approval,
- all required evidence ZIPs exist,
- no destructive migration is pending,
- production DB/R2/Workers were not touched.

## Required final checks

Run and record:

- build,
- typecheck,
- targeted ESLint,
- contracts,
- Unicode/encoding checks,
- product runtime verification,
- workorder live E2E,
- fixture E2E,
- signup return E2E or manual verified status,
- PDF generate/view/render evidence,
- desktop/mobile/iPad evidence,
- console error count,
- page error count,
- failed request count,
- HTTP 4xx/5xx count,
- DB/R2 mutation and residual status,
- Worker changed/deployed status,
- `git diff --check`.

## Metadata requirements

Final repo-state must include:

- Product Completion Level,
- Product Verified,
- Manual QA Status,
- Workorder request counts,
- PDF page PNG,
- QA Evidence ZIP,
- Remaining Manual QA,
- same-Google OAuth return status,
- policy consent hash status,
- Worker changed/deployed,
- 0.24.35 status,
- Final Residual DB Rows,
- Final Residual R2 Objects.

Do not leave `not provided` for relevant fields. Use:

- `PASS`,
- `NOT_RUN`,
- `NOT_APPLICABLE`,
- `DEFERRED`,
- `PENDING_USER_REVIEW`,
- `BLOCKED`.

## Commit rules

Only after all final conditions pass:

- explicit-path stage only,
- no `git add .`,
- exclude artifacts, screenshots, evidence ZIPs, traces, credentials, storageState, `.env*.local`,
- print staged exact set,
- verify `commit-meta.md` required tokens,
- run `git diff --cached --check`,
- commit,
- push origin/master,
- confirm master = origin/master, ahead 0 / behind 0, working tree clean.

Suggested commit:

`0.24.34.5 실제 작업지시서·가입신청·대시보드·PDF 제품 검증 완료`

## Next version

After successful final closeout, next official feature remains:

`0.24.35 — Company-wide Export Execution`

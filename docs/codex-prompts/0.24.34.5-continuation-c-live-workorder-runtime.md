# Codex Prompt — 0.24.34.5 Continuation C — Live Workorder Runtime and Factory Save

Status: `READY_FOR_CODEX`
Baseline version: `0.24.34.6`
Target implementation stream: `0.24.34.5 continuation`
Do not start: `0.24.35 — Company-wide Export Execution`

## Mandatory canonical read order

Before doing any implementation, read these documents in order:

1. `AGENTS.md`
2. `docs/codex-current-state.md`
3. `docs/project/26-final-policy-decisions-and-master-todo.md`
4. `docs/project/31-pre-codex-integrated-master-plan.md`
5. `docs/project/32-product-completion-and-ui-evidence-standard.md`
6. `docs/project/33-workorder-pdf-size-dashboard-finalization.md`
7. `docs/audits/0.24.34.6-system-admin-screen-gap-audit.md`
8. `docs/audits/0.24.34.6-customer-workspace-screen-gap-audit.md`
9. `docs/productization-roadmap.md`
10. `lib/internal/roadmap/roadmap-0.24.34.6.ts`
11. this prompt document
12. any source files and tests named below

Rules:

- Preserve the current dirty work if this is run as a continuation. Do not reset, checkout, restore, stash, or clean unless the owner explicitly requests it.
- Do not start `0.24.35`.
- Do not use Full Reset to hide legacy data or runtime defects.
- Do not access production DB/R2/Workers.
- Do not call work complete from build/typecheck/contract alone.
- User-visible UI work needs localhost browser evidence, screenshots, console/network counts, and a requirements matrix.
- For PDF and screen layout work, stop for owner visual review before final commit/push if the resulting visual output has not already been approved.

## Objective

Verify and fix the real dev/test workorder flow, especially legacy/existing workorder selection, factory save, generated PDF attachment refresh, and error-state behavior.

## Known owner-reported defects

- Existing workorder selection can hang or enter a loading state.
- New workorder creation may render while existing record detail path fails.
- Factory list loads, but factory save may fail with an English 404-style message.
- After save failure the workorder can return to a loading state.
- Generated PDF viewer previously failed or showed unreadable PDF.

## Required live-dev/test scenarios

Use actual dev/test session and DB/API paths. Do not rely only on mocked route fixtures.

1. `/workspace/workorders` initial load.
2. Existing workorder select.
3. Middle detail panel renders.
4. Right panel renders.
5. Factory list loads.
6. Factory select/save.
7. Save succeeds and persists after refresh.
8. Save failure fixture shows Korean error and does not break the screen.
9. Generated incomplete PDF appears in generated documents/attachments.
10. PDF viewer opens.
11. Different existing workorder can be selected after an error.
12. No spinner persists beyond the defined timeout.

## Investigation requirements

Capture:

- workOrderId shape,
- list API URL/status,
- detail API URL/status,
- factory save URL/method/status,
- whether 404 is route 404 or resource 404,
- public/internal ID conversion,
- response body shape,
- server log summary,
- client console/page errors,
- loading state transitions.

Mask sensitive IDs where possible.

## Required UI behavior

- List remains usable if detail fails.
- Detail error is scoped to detail panel.
- Right-panel error is scoped to right panel.
- Factory save pending/error is scoped to factory instruction panel.
- PDF failure is scoped to generated-document/PDF area.
- All user-facing errors are Korean.
- Raw English error text and internal status codes are not shown to customers.

## Verification

Run:

- workorder live product E2E,
- workorder fixture E2E,
- PDF viewer contract,
- factory save scenario,
- desktop/mobile/iPad screenshots.

Final evidence must distinguish:

- `LIVE_DEV_TEST`
- `FIXTURE_E2E`

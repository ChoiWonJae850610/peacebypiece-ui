# 0.24.34.14 Codex 0.24.35 Start Gate

## Status

- Current GPT checkpoint: `0.24.34.14`.
- Owner-provided `0.24.34.13` build/repo-state was checked before this patch.
- Build status from owner log: Next.js production build passed, TypeScript passed, static generation completed `66/66`.
- Repo-state status from owner log: `master = origin/master`, working tree clean, pushed, `APP_VERSION: 0.24.34.13`.
- `0.24.35` Export implementation has not started.

## Purpose

This document is the final GPT-side start gate before Codex begins the `0.24.35` implementation window. It does not implement Export, PDF, DB, R2, size, dashboard, or signup runtime changes.

## Owner approval gate

Codex must not start `0.24.35` until the owner explicitly approves the start of implementation after this checkpoint.

Before approving `0.24.35`, confirm:

1. `0.24.34.14` build passes.
2. repo-state prints `APP_VERSION: 0.24.34.14`.
3. `master = origin/master` and working tree is clean.
4. source ZIP excludes runtime/build/evidence artifacts.
5. `tools/pipeline/download-watcher.ps1` and `tools/pipeline/dev-server-watcher.ps1` remain canonical watcher files.
6. no versioned temporary watcher file remains in source.
7. QA evidence ZIP remains separate from source ZIP.

## Mandatory Codex read order

Every Codex implementation prompt must begin with the canonical read block and read these files in order:

1. `AGENTS.md`
2. `docs/codex-current-state.md`
3. `docs/project/26-final-policy-decisions-and-master-todo.md`
4. `docs/project/31-pre-codex-integrated-master-plan.md`
5. `docs/project/32-product-completion-and-ui-evidence-standard.md`
6. `docs/project/33-workorder-pdf-size-dashboard-finalization.md`
7. latest relevant `docs/audits/0.24.34.x-*.md`
8. target `lib/internal/roadmap/roadmap-*.ts`
9. older documents only for historical context

Confirmed decisions marked `CONFIRMED` must not be re-asked. If documents conflict, the latest canonical decision wins.

## Continuation sequence

Codex must execute the continuation prompts in order:

1. A — Factory-delivery workorder PDF template
2. B — Size standards and measurement UI
3. C — Live workorder runtime and factory save
4. D — Returning signup applicant and policy modal
5. E — Customer dashboard and system admin screens
6. Final — final review, evidence check, commit, push

Do not combine A~E into one large implementation. Do not start Final until A~E are completed and owner review is complete. Final is not a feature implementation prompt.

## Packaging contract

Patch ZIPs must be flat and use encoded file names. `commit-meta.md` must contain these exact labels:

- `Version :`
- `Summary :`
- `Description :`
- `수정 파일 목록 :`
- `추가 파일 목록 :`
- `삭제 파일 목록 :`

Do not use the old `변경 파일 목록 :` token. Do not place pending-test bullets below `삭제 파일 목록 :`.

## Source/evidence separation

Source ZIP must exclude runtime/build/evidence artifacts including:

- `.git/`
- `.next/`
- `node_modules/`
- `artifacts/`
- `playwright-report/`
- `test-results/`
- `reports/`
- `coverage/`
- `blob-report/`
- `.nyc_output/`
- `*.tsbuildinfo`
- `*.zip`
- `storageState*.json`
- `*.har`
- `*.webm`

QA evidence ZIP must remain outside source ZIP and Git unless an explicit evidence artifact is requested separately.

## 0.24.35 boundary

`0.24.35` may begin only after this start gate is applied and owner approval is given. The first Codex action must be continuation A, not a broad implementation.

## Stop conditions

Stop before Codex implementation if any of the following occurs:

- build fails after applying this gate.
- repo-state APP_VERSION is not `0.24.34.14`.
- source ZIP contains evidence/runtime/build artifacts.
- watcher or dev-server watcher fails to run in Windows.
- Codex attempts to merge A~E into one implementation.
- Codex starts Final before A~E completion and owner review.

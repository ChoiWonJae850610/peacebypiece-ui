# 0.24.34.12 Codex Prompt Synchronization

## Purpose

This GPT-side checkpoint synchronizes the Codex continuation prompts and canonical packaging rules before any `0.24.35` Export implementation starts.

## Verified baseline before this patch

Owner-provided `0.24.34.11` results confirmed:

- `npm run build` completed successfully.
- TypeScript passed.
- Static page generation completed `66/66`.
- `master` matched `origin/master`.
- Working tree was clean.
- Push was completed.
- `APP_VERSION` was reported as `0.24.34.11`.

## Canonical read order

Codex must read the following in order before implementation:

1. `AGENTS.md`
2. `docs/codex-current-state.md`
3. `docs/project/26-final-policy-decisions-and-master-todo.md`
4. `docs/project/31-pre-codex-integrated-master-plan.md`
5. `docs/project/32-product-completion-and-ui-evidence-standard.md`
6. `docs/project/33-workorder-pdf-size-dashboard-finalization.md`
7. Latest relevant `docs/audits/0.24.34.x-*.md`
8. Target `lib/internal/roadmap/roadmap-*.ts`
9. Older project documents only when historical context is needed

Confirmed policy decisions must not be re-asked. Later canonical decisions override older conflicting documents.

## Continuation execution guard

Codex continuation prompts must run in this order:

1. A — Factory-delivery workorder PDF template
2. B — Size standards and measurement UI
3. C — Live workorder runtime and factory save
4. D — Returning signup applicant and policy modal
5. E — Customer dashboard and system admin screens
6. Final — final review, evidence check, commit, push

Rules:

- Do not merge A~E into one large implementation.
- Do not run a later prompt before the previous checkpoint is completed.
- Final is not a feature implementation prompt.
- Final is for verification, evidence review, commit, and push only.
- `0.24.35` Export remains blocked until owner approval.

## Patch packaging contract

Patch ZIP files must remain flat and use path-encoded filenames such as `docs__...`, `lib__...`, and `tools__...`.

`commit-meta.md` must include these exact tokens:

```text
Version :
Summary :
Description :
수정 파일 목록 :
추가 파일 목록 :
삭제 파일 목록 :
```

Do not use the old token:

```text
변경 파일 목록 :
```

Do not place pending tests or general bullets under `삭제 파일 목록 :`.

## Source and evidence separation

Source ZIP must exclude:

- `.git/`
- `.next/`
- `node_modules/`
- `artifacts/`
- `playwright-report/`
- `test-results/`
- `reports/`
- `coverage/`
- `blob-report/`
- `.nyc_output/`
- `*.tsbuildinfo`
- `*.zip`
- `storageState*.json`
- `*.har`
- `*.webm`

QA evidence must be saved separately and must not be mixed into source ZIPs.

## Scope

This checkpoint only updates documentation, prompt guardrails, version, and roadmap records.

## Out of scope

- `0.24.35` Export implementation
- PDF template implementation
- DB/R2 schema or runtime changes
- Browser QA execution
- PowerShell pipeline logic changes

## Next step

After this patch is applied and verified, the next GPT checkpoint is `0.24.34.13`, focused on final Codex prompt consistency review or the owner-approved `0.24.35` start gate decision.

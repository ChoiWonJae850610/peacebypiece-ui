# 0.24.34.7 Project Structure and Cleanup Audit

Status: CLEANUP AUDIT ONLY  
Baseline: 0.24.34.6  
Result version: 0.24.34.7  
Scope: Identify cleanup candidates without destructive deletion

## 1. Immediate cleanup result

0.24.34.7 does not delete source folders or old documents.

The only direct cleanup implementation in this version is pipeline-level packaging guard correction.

## 2. Cleanup candidates that are safe to document, not delete

### 2.1 Generated output in source handoff

These are not source files and should not appear in source ZIPs:

- `artifacts/`
- `playwright-report/`
- `test-results/`
- `reports/`
- `tsconfig.tsbuildinfo`
- generated ZIP files
- repo-state files
- build-result files

Action: exclude from generated source ZIPs. Do not delete evidence unless owner explicitly confirms.

### 2.2 Korean docs folder names shown as broken in Linux extraction

In ChatGPT/Linux ZIP extraction, some Korean folder names may appear mojibake-like.

Owner confirmed Windows Explorer and Git display the names correctly.

Policy:

- Do not rename these folders from GPT/Linux inspection alone.
- Treat this as ZIP filename encoding observation.
- Codex/Windows should check `git ls-files docs` and PowerShell `Get-ChildItem docs`.
- Rename only if the repository itself actually contains broken names.

### 2.3 Lockfile duplication

The repository contains both:

- `package-lock.json`
- `pnpm-lock.yaml`

Policy:

- Do not delete either file in 0.24.34.7.
- Confirm canonical package manager before removing a lockfile.
- Existing project execution has generally followed npm, but that is not enough for a destructive cleanup.

### 2.4 Reports and historical evidence

`reports/` and previous QA output may be useful as historical evidence.

Policy:

- Exclude from source ZIP.
- Keep deletion as a separate owner-approved cleanup task.

### 2.5 Old docs and archive candidates

Large numbers of older docs may be retained for traceability.

Policy:

- Do not bulk-delete docs.
- If cleanup is needed, create a nav/index or archive plan first.
- Canonical docs must continue to use full filenames, especially where duplicate numbers exist.

## 3. Duplicate document number observation

Known duplicate numbering:

```text
docs/project/32-pre-codex-authority-consistency-gate.md
docs/project/32-product-completion-and-ui-evidence-standard.md

docs/project/33-public-signup-schema-repository-prep-0.24.26.md
docs/project/33-workorder-pdf-size-dashboard-finalization.md
```

Policy:

- Avoid referring to these as only "32" or "33".
- Use full filenames in Codex prompts and current-state docs.
- Rename only after full reference search.

## 4. Delete-prohibited list for now

Do not delete or rename in this patch:

- Korean docs folders
- `pnpm-lock.yaml`
- `reports/`
- old audit docs
- large components
- dev/test console files
- system routes
- generated QA evidence in working tree

## 5. Next cleanup path

Recommended order:

1. Keep pipeline source ZIP excludes fixed.
2. Add a package-manager decision note before lockfile cleanup.
3. Add a docs archive index before moving old docs.
4. Split large files only after active 0.24.34.x productization checkpoints are complete.
5. Perform WAFL raw UI cleanup route by route, not globally.

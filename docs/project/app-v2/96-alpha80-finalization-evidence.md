# Alpha.80 Public Share Lifecycle, Security, and Finalization Evidence

Document type: **Immutable Evidence**

Finalization status: `ALPHA80_FINALIZATION_COMPLETE`

Accepted product checkpoint: `ALPHA80_COMPLETE`

Owner actual Final Combined Smoke is `PASS` on iPhone and iPad mini. Regular/Large iPad and Android actual-device
execution remain `NOT_RUN` and are not inferred PASS.

## Accepted boundary

- One exact healthy current-Revision newest generated artifact binds company, WorkOrder, Revision, document, generation,
  immutable PDF metadata, and one canonical Maker-current Share lineage. Repeated Share reuses the active link and
  duplicate current links remain zero.
- Database time owns expiry. Exact revoke and expiry are terminal, create no automatic replacement, and permit only one
  explicit successor whose predecessor never reactivates. The existing three-day TTL is unchanged.
- Open, Copy, native Share, and Revoke consume one exact current URL/token identity. Read-only actions create, rotate, or
  mutate nothing, and terminal current actions disappear while top-level Share remains the explicit successor entry.
- The 256-bit HMAC-SHA256 credential remains hash-only at rest and resolves one immutable artifact. Malformed, mutated,
  cross-resource, terminal, and replay requests fail closed without sensitive identity leakage. Public Viewer and cache
  authorization remain minimized and terminal-aware.
- Alpha.79 missing/corrupt/revoked/deleted document lifecycle authority remains superior to Share lifecycle. Drawing
  Scene v1, WORLD `1000×1400`, output `1500×2100`, and Camera-independent output remain unchanged.

## Finalization boundary

- APP_VERSION: `2.0.0-alpha.80`
- migration: `22/22`; new/Production migration `0/0`
- dependency/native/config/EAS: `0/0/0/0`
- API/schema/billing/Factory delta: `0/0/0/0`
- Production/Owner/ambiguous business mutation: `0/0/0`
- Production R2 destructive mutation: `0`
- tag/release: `0/0`
- finalization behavior delta: `0`

Post-version verification, final commit/origin synchronization, Runtime identity, final Source ZIP identity, and exact
completion time are owned by the matching post-push repo-state and Official Result/QA pair because a tracked source file
cannot contain the identity of the commit that contains it.

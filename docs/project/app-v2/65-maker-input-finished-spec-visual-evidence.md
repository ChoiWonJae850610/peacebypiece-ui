# Alpha.65 Maker Input, Production and Finished Spec Visual Evidence

Document type: **Immutable Evidence**

Status: `ALPHA65_FINALIZATION_COMPLETE`

Product checkpoint: `ALPHA65_MAKER_INPUT_FINISHED_SPEC_VISUAL_COMPLETE`

Result version: `2.0.0-alpha.65`

## Accepted result

Alpha.65 finalizes the cumulative owner-approved Maker candidate on the alpha.64 document and
mobile architecture baseline. The release keeps one canonical `WaflInputSheet` root for mounted
gesture, continuous free-settle, atomic entrance, body viewport, focus reveal, safe area, footer,
keyboard and nested presentation responsibilities. `WaflReelPickerSheet` owns the shared
WAFL PICK reel path, while Size/Color/Spec direct creation shares one explicit reusable-create
entry and form grammar. Required choices stage a real initial value where safe; nullable and
new-Process `미지정` semantics remain explicit and non-persistable.

Finished Spec retains WorkOrder Size as its sole Size source of truth. The accepted UI supports
category-aware WAFL/company spec items, direct reusable creation, saved-spec operations, decimal
centimetres and exact one-eighth-inch display/input, zero-row draft bootstrap, and geometry-stable
editable table cells. Address Search remains an in-app authenticated Juso flow with read-only
postal/basic-address surfaces and editable detail/contact fields.

Production authoring replaces the historical read-only presentation with one draft factory and
zero-to-many additional processes. Factory/process/eligible-partner fields use canonical PICK;
unit labour cost and process-row memo use canonical inline editing and a serialized current-
identity/version queue. WorkOrder total quantity remains the server source of truth for process
quantity and amount aggregates. Memo ownership, request/cancel/complete lifecycle, tenant/lock,
idempotency, Event and Receipt behavior remain independent of presentation. Image/Attachment no
longer edits the historical revision factory-delivery memo, but existing stored values and
document/PDF consumers are preserved.

The visual Finished Spec selector separates fixed garment recognition from measurement feedback.
It uses eight authored static T/B/O/D front/back SVG views with exact TypeScript mirrors, one
ephemeral focused preview at most, unrestricted staged grid selection, and `55/55` stable
front/back routes. The garment-only state contains no measurement overlay. The current T/D round
necklines, simplified Outer neck/collar and straight pockets retain the prior continuous
shoulder/armhole/sleeve construction; Lower remains byte-stable.

## Owner acceptance and deferred asset boundary

The owner authorized release under:

`OWNER_RELEASE_ACCEPTED_WITH_PROVISIONAL_GARMENT_ASSETS`

This is not a claim that the package-authored garment art is the final aesthetic target.
Designer-authored Illustrator SVG replacement for the same eight views is deferred as:

`DESIGNER_AUTHORED_GARMENT_SVG_REPLACEMENT_DEFERRED`

That future asset-only package must preserve the current renderer, front/back pair, focused
preview cardinality `0..1`, `55/55` routing, four-column grid, X/V staging and persistence
boundaries.

## Runtime, safety and verification boundary

- Canonical verification uses Windows Node `24.14.0`; the pre-version permanent inventory is
  `156/156 PASS` with failure/skip `0/0`.
- The canonical physical-QA profile is the cumulative `alpha65-current-maker` capability owner.
- Port 3000 and forbidden-tunnel gates are provenance-aware: WAFL-owned or unresolved ownership
  blocks; exact-provenance unrelated services remain untouched.
- Migration ledger: `18/18`; migration `019` and schema expansion: `0`.
- Production mutation and owner-fixture mutation: `0/0`.
- Dependency/native/EAS expansion: `0`.
- Force push, amend, reset, restore, clean, stash, rebase and history rewrite: `0`.
- Final post-version Verify, commit, pushed HEAD/origin, clean Git, release ZIP and repo-state
  identities are owned by the matching post-push alpha.65 repo-state because tracked evidence
  cannot contain the hash of the commit that contains itself.

## Next boundary

`2.0.0-alpha.66` is `NOT_STARTED`. Its candidate label is
`Derived WorkOrder architecture foundation`; this evidence grants no alpha.66 product scope or
implementation authority.

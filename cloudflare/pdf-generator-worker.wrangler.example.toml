# Deprecated example.
# Use cloudflare/pdf-generator-worker/wrangler.toml instead.
#
# The PDF generator requires @cloudflare/puppeteer and Browser Rendering binding.
# Deploy with:
#   cd cloudflare/pdf-generator-worker
#   npm ci
#   npx wrangler secret put WAFLOW_PDF_GENERATOR_TOKEN
#   npx wrangler deploy

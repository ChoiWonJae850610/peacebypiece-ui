# Pending Tests — 0.23.39

## 수동 Build
- npm run build

## 작업지시서 PATCH 결과 계약
- 분류 변경 후 납기일·담당자·수량·재고가 유지되는지
- 납기일 변경 후 분류·담당자·수량·재고가 유지되는지
- 담당자 변경 후 분류·납기일·수량·재고가 유지되는지
- 수량 변경 후 분류·납기일·담당자·재고가 유지되는지
- 새로고침 후 변경한 필드만 DB 값으로 유지되는지
- 저장 성공 응답에 요청하지 않은 기본정보가 포함되지 않는지
- loading → success/error 토스트 순서가 기존 공통 mutation 정책을 유지하는지

## 공통 계약 회귀
- 작업지시서 전체 저장과 상태 변경이 정상 동작하는지
- 작업지시서 자재·공정·외주 변경 시 상세 데이터가 유지되는지
- 발주서 저장 mutation 직렬화 동작이 유지되는지
- 관리자·일반 멤버 권한 정책이 유지되는지

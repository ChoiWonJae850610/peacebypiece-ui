# Pending Tests — 0.23.41

## 수동 Build
- npm run build

## 작업지시서 단일 PATCH 회귀
- 분류만 변경한 뒤 납기일·담당자·수량·재고가 유지되는지
- 납기일만 변경한 뒤 분류·담당자·수량·재고가 유지되는지
- 담당자만 변경한 뒤 분류·납기일·수량·재고가 유지되는지
- 수량만 변경한 뒤 분류·납기일·담당자·재고가 유지되는지
- 재고 수량만 변경한 뒤 분류·납기일·담당자·수량이 유지되는지
- 각 항목 변경 후 새로고침해도 나머지 값이 DB에 유지되는지

## 명시적 초기화
- 담당자 해제 시 manager / manager_id만 초기화되는지
- 납기일 삭제 시 due_date만 NULL 처리되는지
- 재고 0 입력 시 inventory_quantity만 0으로 저장되는지

## 공통 PATCH 규칙
- `undefined` 필드가 SQL assignment에 포함되지 않는지
- `null` 필드는 명시적 초기화로 반영되는지
- 실제 요청 필드와 last_saved_at만 UPDATE되는지

## 기존 기능 회귀
- 작업지시서 상태 변경
- 공장 전달사항 저장
- 작업지시서 자재·공정·외주 저장
- 발주서 자재 종류·공급처·납기일 저장

# Pending Tests — 0.23.40

## 수동 Build
- npm run build

## 작업지시서 기본정보 유지
- 분류 변경 후 납기일·담당자·수량·재고가 유지되는지
- 납기일 변경 후 분류·담당자·수량·재고가 유지되는지
- 담당자 변경 후 분류·납기일·수량·재고가 유지되는지
- 수량 변경 후 분류·납기일·담당자·재고가 유지되는지
- 각 항목을 연속으로 변경해도 이전 값이 사라지지 않는지
- 새로고침 후 변경한 값과 기존 값이 모두 유지되는지

## 상세 로딩 경쟁 상태
- 작업지시서 선택 직후 상세 로딩 중 기본정보를 변경해도 값이 되돌아가지 않는지
- 느린 상세 응답이 저장 완료 후 도착해도 최신 기본정보를 덮지 않는지
- 상세 자재·공정·외주·첨부 데이터는 정상 로딩되는지

## 공통 상태 병합
- 담당자 저장 성공 후 persisted 상태가 다른 필드를 잃지 않는지
- 로컬 상태와 persisted 상태가 동일한 기본정보를 유지하는지
- loading → success/error 토스트가 기존 mutation 정책대로 동작하는지

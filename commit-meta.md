# Version
0.6.6433 → 0.7.0

# Summary
WorkOrder PC 레이아웃 통일 1차

# Description
작업지시서 PC 화면의 외곽 레이아웃을 관리자 화면과 유사한 고정 좌측 패널, 중앙 본문, 우측 보조 패널 구조로 정리하고 앱 버전을 0.7.0으로 갱신함.

# 수정 파일 목록
- `components/workorder/layout/DesktopWorkspaceLayout.tsx` — PC 전용 작업지시서 레이아웃을 관리자 화면 톤에 맞춰 고정 폭 좌측 패널, 중앙 본문, 우측 패널 구조로 정리
- `lib/constants/app.ts` — APP_VERSION 값을 0.7.0으로 동기화
- `commit-meta.md` — 모바일 최소 응답용 작업 메타데이터 갱신

# 추가 파일 목록
없음

# 삭제 파일 목록
없음

# 작업 상세 내용
- WorkOrder PC 레이아웃 배경을 관리자 화면과 같은 계열의 그라데이션 배경으로 정리함.
- 좌측 작업지시서 목록 패널 폭을 관리자 사이드바 기준과 동일한 268px로 고정함.
- 중앙 상세 영역은 최대 폭을 제한해 화면 전환 시 본문 폭이 과하게 넓어지는 느낌을 줄임.
- 우측 첨부/메모 패널은 고정 폭 카드형 영역으로 정리함.
- 모바일/태블릿 레이아웃 파일은 수정하지 않음.
- 기능, 권한, 상태, actionFlow, selector 로직은 수정하지 않음.

# 이번 작업 진행 판단
0.7.0 목표인 WorkOrder PC 레이아웃 통일 1차를 최소 범위로 반영함. 구조 분해나 권한 로직 변경은 하지 않았으며, 다음 단계에서 WorkOrderDetail 구조 점검으로 이어갈 수 있음.

# 다음 작업 권장 버전
0.7.1 — WorkOrderDetail 구조 분해 준비

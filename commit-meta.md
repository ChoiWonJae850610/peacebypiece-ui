Version : 0.9.35 → 0.9.36

Summary : 통계 실사용 구조 보강 1차

Description :
- 통계 기간 선택에 누적 보기를 추가했습니다.
- 7일, 15일, 30일, 이번달, 누적 기준을 같은 기간 선택 UI에서 고를 수 있도록 정리했습니다.
- 통계 화면에 월별 기준과 누적 기준의 의미를 설명하는 운영용 안내 카드를 추가했습니다.
- 누적 보기 선택 시 DB 조회의 updated_at 기간 제한을 제거해 전체 기간 기준 통계를 볼 수 있도록 보완했습니다.
- 한국어/영어 i18n에 누적 기간과 월별/누적 기준 안내 문구를 추가했습니다.
- APP_VERSION 값을 0.9.36으로 동기화했습니다.
- npm 빌드는 사용자 요청에 따라 실행하지 않았습니다.

수정 파일 목록 :
- lib/constants/app.ts : APP_VERSION 값을 0.9.36으로 갱신했습니다.
- lib/admin/stats/types.ts : 통계 기간 타입에 all 누적 기준을 추가했습니다.
- lib/admin/stats/selectors.ts : 통계 기간 정규화와 기간 선택 버튼에 누적 옵션을 추가했습니다.
- lib/admin/adminStats.repository.ts : 누적 보기 선택 시 기간 where 조건을 제거하도록 보완했습니다.
- components/admin/dashboard/AdminStatsDashboard.tsx : 통계 화면에 월별/누적 기준 안내 카드를 추가하고 누적 라벨 번역 매핑을 추가했습니다.
- lib/i18n/ko/admin.ts : 통계 화면의 누적 기간과 월별/누적 기준 안내 한국어 문구를 추가했습니다.
- lib/i18n/en/admin.ts : 통계 화면의 누적 기간과 월별/누적 기준 안내 영어 문구를 추가했습니다.
- commit-meta.md : 모바일 최소 응답 모드용 산출물 메타 문서를 0.9.36 기준으로 갱신했습니다.

추가 파일 목록 :
- 없음

삭제 파일 목록 :
- 없음

작업 상세 :
- 기존 0.9.35 통계 화면은 7일, 15일, 30일, 월별 중심이었고 지금까지 누적 데이터와 선택 기간 데이터의 의미가 화면에서 분리되지 않았습니다.
- 이번 작업에서는 누적 기준을 period=all 쿼리로 분리하고, 해당 기준에서는 updated_at 기간 제한을 적용하지 않도록 repository 계층에서 처리했습니다.
- 통계 UI 상단에 월별 기준과 누적 기준의 의미를 설명하는 카드를 추가해, 완료/지연/불량 같은 운영 지표와 생산 단계 비율 같은 누적성 지표를 구분해서 이해할 수 있게 했습니다.
- 아직 실제 DB에 별도 통계 이벤트 테이블을 두고 삭제된 작업지시서의 1차/2차/3차 기록을 영구 보존하는 구조까지는 포함하지 않았습니다. 이 부분은 다음 DB 스키마 작업에서 별도 통계 이벤트/스냅샷 테이블로 확장하는 것이 맞습니다.
- package.json 및 package-lock.json은 수정하지 않았습니다.

검증 :
- npm 빌드는 사용자 요청에 따라 실행하지 않았습니다.
- commit-meta.md 파일에 Version, Summary, Description, 수정 파일 목록, 추가 파일 목록, 삭제 파일 목록 항목과 콜론(:) 토큰이 포함된 것을 확인했습니다.
- APP_VERSION 값이 0.9.36으로 반영된 것을 확인했습니다.

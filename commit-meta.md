Version : 0.9.32 → 0.9.33

Summary : 저장소 관리 UI 잔여 정리 및 i18n 안정화

Description :
- 저장소 관리 화면의 첨부량 그래프와 파일유형 도넛 그래프 크기를 확대했습니다.
- 저장소 요약 카드 값을 한글 라벨 검색이 아니라 고정 카드 순서 기준으로 읽도록 정리했습니다.
- 저장소 탭 라벨을 i18n 키 기준으로 표시하도록 보강했습니다.
- 파일유형 도넛 그래프의 문서, 디자인, 기타 라벨이 언어 전환에 맞게 표시되도록 정리했습니다.
- APP_VERSION 값을 0.9.33으로 동기화했습니다.
- npm 빌드는 사용자 요청에 따라 실행하지 않았습니다.

수정 파일 목록 :
- lib/constants/app.ts : APP_VERSION 값을 0.9.33으로 갱신했습니다.
- app/admin/files/page.tsx : 저장소 탭 라벨을 i18n 키로 변환하는 표시 헬퍼를 추가했습니다.
- components/admin/files/FileStorageSummary.tsx : 그래프 크기를 확대하고 저장소 요약값/파일유형 라벨 처리 방식을 보강했습니다.
- lib/i18n/ko/admin.ts : 저장소 탭과 파일유형 기타 라벨의 한국어 문구를 추가했습니다.
- lib/i18n/en/admin.ts : 저장소 탭과 파일유형 기타 라벨의 영어 문구를 추가했습니다.
- commit-meta.md : 모바일 최소 응답 모드용 산출물 메타 문서를 0.9.33 기준으로 작성했습니다.

추가 파일 목록 :
- 없음

삭제 파일 목록 :
- 없음

작업 상세 :
- 기존 저장소 요약 영역은 첨부량/파일유형 그래프가 상대적으로 작아 우측 영역의 시각 정보가 약했습니다. 그래프 컨테이너 높이와 SVG 크기를 키워 저장소 화면에서 바로 읽히도록 조정했습니다.
- 기존 첨부파일/휴지통/보관기간 값 추출은 한글 라벨 포함 여부에 의존했습니다. 언어 전환 시 깨질 수 있으므로 usageCards 배열의 고정 카드 순서 기준으로 읽도록 변경했습니다.
- 기존 탭 버튼은 snapshot 데이터의 label을 그대로 출력했습니다. 저장소 탭 이름이 영문 전환에 맞게 바뀌도록 filesPage.tabs i18n 키를 추가하고 화면에서 해당 키를 사용하도록 변경했습니다.
- 파일유형 도넛 그래프는 DB나 snapshot에서 한글 라벨이 들어와도 현재 언어 기준 문구로 표시되도록 변환 헬퍼를 추가했습니다.
- package.json 및 package-lock.json은 수정하지 않았습니다.

검증 :
- npm 빌드는 사용자 요청에 따라 실행하지 않았습니다.
- commit-meta.md 파일에 Version, Summary, Description, 수정 파일 목록, 추가 파일 목록, 삭제 파일 목록 항목과 콜론(:) 토큰이 포함된 것을 확인했습니다.
- APP_VERSION 값이 0.9.33으로 반영된 것을 확인했습니다.
